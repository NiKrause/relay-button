# GitHub Action Consumer Example

This example directory is reserved for a minimal external repository consuming:

- the shared `aleph-vm-deploy` GitHub Action
- the future shared reusable workflow

The goal is to show the real external-consumer contract after
`shared-aleph-tooling` is split into its own repository.
